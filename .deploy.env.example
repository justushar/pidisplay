# Copy to .deploy.env (gitignored) and edit. Both are optional.
#
# Leave PI_PASS unset to use SSH key authentication, which is preferable —
# deploy.sh only falls back to password auth when PI_PASS is non-empty.

PI_HOST=pi@raspberrypi.local
#PI_PASS=
